# Workcell Wayfinder

Canonical ticket map: GitHub issue #1.

```text
#2 public contract + reference tracer bullet
  -> #3 replaceability / degradation / lifecycle
      -> #4 workspace provider
      -> #5 container provider (Factory #24 gate)
  -> #6 cross-repo conformance (Factory #113 gate)
      -> #7 Candidate material-reality integration
```

The current executable frontier is #2. Factory #57 remains blocked by its own dependencies and is not treated as the Workcell code-location ticket.
